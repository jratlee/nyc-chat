# Base URL of the FastAPI backend (legal_api_server.py).
# Defaults to http://localhost:8005 when unset. For Codespaces or production,
# set this to the forwarded/public backend URL.
VITE_API_BASE_URL=http://localhost:8005
